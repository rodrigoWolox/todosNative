import { StyleSheet } from 'react';

const styles = StyleSheet.create({
  footer: {
    paddingVertical: 15,
    alignItems: 'center'
  },
  remove: {
    color: '#CD5C5C'
  }
})

export default styles;
