import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    paddingVertical: 15,
    alignItems: 'center',
    bottom: 0
  },
  remove: {
    color: '#CD5C5C',
    fontSize: 15
  }
})

export default styles;
