import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    paddingVertical: 15,
    alignItems: 'center'
  },
  remove: {
    color: '#CD5C5C'
  }
})

export default styles;
